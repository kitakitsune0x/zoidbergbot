#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
IMAGE_NAME="${ZOIDBERGBOT_INSTALL_E2E_IMAGE:-${CLAWDBOT_INSTALL_E2E_IMAGE:-zoidbergbot-install-e2e:local}}"
INSTALL_URL="${ZOIDBERGBOT_INSTALL_URL:-${CLAWDBOT_INSTALL_URL:-https://zoidbergbot.bot/install.sh}}"

OPENAI_API_KEY="${OPENAI_API_KEY:-}"
ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-}"
ANTHROPIC_API_TOKEN="${ANTHROPIC_API_TOKEN:-}"
ZOIDBERGBOT_E2E_MODELS="${ZOIDBERGBOT_E2E_MODELS:-${CLAWDBOT_E2E_MODELS:-}}"

echo "==> Build image: $IMAGE_NAME"
docker build \
  -t "$IMAGE_NAME" \
  -f "$ROOT_DIR/scripts/docker/install-sh-e2e/Dockerfile" \
  "$ROOT_DIR/scripts/docker/install-sh-e2e"

echo "==> Run E2E installer test"
docker run --rm \
  -e ZOIDBERGBOT_INSTALL_URL="$INSTALL_URL" \
  -e ZOIDBERGBOT_INSTALL_TAG="${ZOIDBERGBOT_INSTALL_TAG:-${CLAWDBOT_INSTALL_TAG:-latest}}" \
  -e ZOIDBERGBOT_E2E_MODELS="$ZOIDBERGBOT_E2E_MODELS" \
  -e ZOIDBERGBOT_INSTALL_E2E_PREVIOUS="${ZOIDBERGBOT_INSTALL_E2E_PREVIOUS:-${CLAWDBOT_INSTALL_E2E_PREVIOUS:-}}" \
  -e ZOIDBERGBOT_INSTALL_E2E_SKIP_PREVIOUS="${ZOIDBERGBOT_INSTALL_E2E_SKIP_PREVIOUS:-${CLAWDBOT_INSTALL_E2E_SKIP_PREVIOUS:-0}}" \
  -e OPENAI_API_KEY="$OPENAI_API_KEY" \
  -e ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" \
  -e ANTHROPIC_API_TOKEN="$ANTHROPIC_API_TOKEN" \
  "$IMAGE_NAME"
