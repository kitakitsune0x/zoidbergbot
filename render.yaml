services:
  - type: web
    name: openclaw
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: ZOIDBERGBOT_STATE_DIR
        value: /data/.openclaw
      - key: ZOIDBERGBOT_WORKSPACE_DIR
        value: /data/workspace
      - key: ZOIDBERGBOT_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: zoidbergbot-data
      mountPath: /data
      sizeGB: 1
